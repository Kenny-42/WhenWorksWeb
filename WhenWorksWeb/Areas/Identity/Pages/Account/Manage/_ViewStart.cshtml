@{
    // Nests every Manage/*.cshtml page inside the sidebar-nav wrapper in
    // _Layout.cshtml (which in turn falls back to the site's shared layout),
    // overriding the plain Areas/Identity/Pages/_ViewStart.cshtml that would
    // otherwise apply and skip the sidebar entirely.
    Layout = "_Layout";
}
